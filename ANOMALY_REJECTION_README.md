# 异常值拒绝实现说明

## 概述

本次实现添加了全面的异常值检测和拒绝机制，用于提高物体定位的鲁棒性和准确性。系统能够自动识别和拒绝异常观测，并在必要时回退到可靠的估计。

## 主要功能

### 1. 观测异常检测 (`_check_observation_anomaly`)

在 `add_observation()` 中添加观测前，进行以下检查：

#### 1.1 Bbox有效性检查
- **无效bbox**：格式错误或无法归一化
- **bbox太小**：面积占比 < 0.05%
- **bbox太大**：面积占比 > 80%
- **bbox中心超出有效区域**：距离图像边缘 < 5%

#### 1.2 深度检查（如果提供了深度图）
- **缺少有效深度值**：bbox区域内无有效深度值（0.1-10米范围）
- **深度太小**：平均深度 < 0.1米
- **深度太大**：平均深度 > 10米

**返回信息：**
- `is_anomaly`: 是否为异常
- `reason`: 异常原因（详细描述）
- `raw_data`: 原始观测数据（用于日志）

### 2. 三角化异常检测

#### 2.1 射线异常检测

在RANSAC三角化之前，检查每条射线：

- **与上一个可靠估计的距离**：
  - 计算射线到上一个可靠估计的距离
  - 如果距离 > 2米，拒绝该射线
  - 记录拒绝原因和原始数据

#### 2.2 估计异常检测

在获得新估计后，检查：

- **与上一个可靠估计的距离**：
  - 如果新估计距离上一个可靠估计 > 2米，标记为可疑
  - 降低置信度（乘以0.5）
  - 记录警告日志

- **内点比例检查**：
  - 如果内点比例 < 30%，标记为可疑
  - 降低置信度
  - 建议收集更多观测

### 3. 回退机制

#### 3.1 观测回退

如果连续多个观测被拒绝：
- **条件**：`anomaly_count >= 3`
- **行为**：回退到最后一个可靠估计
- **置信度**：降低到原来的80%

#### 3.2 射线回退

如果所有射线都被拒绝：
- **行为**：回退到最后一个可靠估计
- **置信度**：降低到原来的70%
- **日志**：记录所有拒绝原因

#### 3.3 三角化失败回退

如果RANSAC三角化失败：
- **行为**：请求多角度重新扫描
- **日志**：记录失败原因和建议

### 4. 可靠估计管理

#### 4.1 更新条件

可靠估计在以下条件下更新：
- **置信度 >= 0.5**
- **不可疑**（未标记为suspect）
- **估计成功**（深度反投影或RANSAC三角化成功）

#### 4.2 存储信息

- `last_reliable_estimate`: 最后一个可靠估计的位置
- `last_reliable_confidence`: 最后一个可靠估计的置信度

## 实现细节

### 异常检测阈值

| 检查项 | 阈值 | 说明 |
|--------|------|------|
| bbox最小面积 | 0.05% | 避免过小的检测框 |
| bbox最大面积 | 80% | 避免过大的检测框 |
| bbox边缘距离 | 5% | 确保中心在有效区域 |
| 深度最小值 | 0.1m | 10厘米 |
| 深度最大值 | 10.0m | 10米 |
| 估计距离阈值 | 2.0m | 与上一个估计的距离 |
| 射线距离阈值 | 2.0m | 射线到上一个估计的距离 |
| 内点比例阈值 | 30% | RANSAC内点比例 |
| 异常计数阈值 | 3 | 触发回退的异常观测数 |

### 置信度调整

- **可疑估计**：置信度 × 0.5
- **回退估计**：置信度 × 0.8（观测回退）或 0.7（射线回退）

### 日志记录

所有异常情况都会记录详细日志：

1. **异常观测**：
   ```
   空间融合模块：异常观测被拒绝 - bbox_too_small (area_ratio=0.0001 < 0.0005), 原始数据: {...}
   ```

2. **射线拒绝**：
   ```
   空间融合模块：拒绝射线 2 - 与上一个可靠估计差异过大 (distance=3.456m > 2.0m)
   ```

3. **可疑估计**：
   ```
   空间融合模块：标记为可疑估计 - diverged_from_last_estimate (distance=2.500m > 2.0m), low_inlier_ratio (0.200 < 0.300)
   ```

4. **回退操作**：
   ```
   空间融合模块：连续 3 个异常观测，回退到最后一个可靠估计
   空间融合模块：回退到可靠估计 (1.234, 2.345, 3.456), 置信度: 0.560
   ```

## 使用示例

### 正常流程

```python
# 添加观测（自动检测异常）
observation = fusion_module.add_observation(
    position=pose["position"],
    rotation=pose["rotation"],
    bbox=bbox,
    confidence=conf,
    label=label,
    img_shape=(rgb.shape[0], rgb.shape[1]),
    depth=depth
)

# 如果观测被拒绝，返回None
if observation is None:
    print("观测被拒绝，可能是异常值")
```

### 估计位置（自动处理异常）

```python
# 自动使用异常检测和回退机制
estimated_pos = fusion_module.estimate_object_position()

if estimated_pos is None:
    # 可能需要多角度重新扫描
    print("估计失败，建议执行多角度重新扫描")
```

## 异常类型

### 观测异常

1. **invalid_bbox**: bbox格式无效
2. **bbox_too_small**: bbox面积太小
3. **bbox_too_large**: bbox面积太大
4. **bbox_center_out_of_valid_region**: bbox中心超出有效区域
5. **missing_valid_depth**: 缺少有效深度值
6. **depth_too_small**: 深度值太小
7. **depth_too_large**: 深度值太大

### 三角化异常

1. **ray_diverged_from_last_estimate**: 射线与上一个估计差异过大
2. **diverged_from_last_estimate**: 新估计与上一个估计差异过大
3. **low_inlier_ratio**: RANSAC内点比例过低

## 回退策略

### 策略1：观测回退

**触发条件**：
- 连续3个观测被拒绝（`anomaly_count >= 3`）
- 三角化失败

**行为**：
- 使用最后一个可靠估计
- 置信度降低到80%

### 策略2：射线回退

**触发条件**：
- 所有射线都被拒绝（与上一个估计差异过大）

**行为**：
- 使用最后一个可靠估计
- 置信度降低到70%

### 策略3：请求重新扫描

**触发条件**：
- 三角化失败
- 没有可靠估计可回退

**行为**：
- 返回None
- 记录日志建议多角度重新扫描

## 优势

1. **鲁棒性**：自动拒绝异常观测，避免错误估计
2. **自适应性**：根据历史估计动态调整阈值
3. **可追溯性**：详细记录所有异常原因和原始数据
4. **回退机制**：在异常情况下仍能提供合理估计

## 参数调优

### 距离阈值

- **默认值**：2.0米
- **调整建议**：
  - 小物体：1.0-1.5米
  - 大物体：2.0-3.0米
  - 动态调整：根据物体大小自适应

### 异常计数阈值

- **默认值**：3
- **调整建议**：
  - 严格场景：2（更早触发回退）
  - 宽松场景：5（允许更多异常）

### 置信度阈值

- **可靠估计更新**：0.5
- **调整建议**：
  - 高精度场景：0.7
  - 宽松场景：0.3

## 未来改进

1. **自适应阈值**：根据物体大小和场景动态调整阈值
2. **多级回退**：支持多个历史估计的回退链
3. **异常模式识别**：识别重复的异常模式
4. **置信度衰减**：可靠估计的置信度随时间衰减
5. **异常统计**：统计异常类型和频率，用于系统优化

## 技术细节

### 点到射线距离计算

使用向量投影方法：
```python
vec_to_point = point - ray_origin
t = dot(vec_to_point, ray_direction)
closest_point = ray_origin + t * ray_direction
distance = ||point - closest_point||
```

### 深度值提取

从bbox区域提取深度值：
```python
depth_roi = depth[y1:y2, x1:x2]
valid_depths = depth_roi[(depth_roi >= 0.1) & (depth_roi <= 10.0) & 
                         (~np.isnan(depth_roi)) & (~np.isinf(depth_roi))]
mean_depth = mean(valid_depths)
```

### 异常计数管理

- **成功添加观测**：重置计数为0
- **观测被拒绝**：计数+1
- **达到阈值**：触发回退

