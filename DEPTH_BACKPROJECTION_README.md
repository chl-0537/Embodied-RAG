# 深度反投影物体定位实现说明

## 概述

本次实现添加了基于深度图的物体定位功能，在三角化之前优先使用深度反投影来获取物体的3D世界坐标。这提供了更直接、更准确的定位方法。

## 主要功能

### 1. 深度传感器

**HabitatAgentWrapper** 中已经包含深度传感器：
- `depth_sensor_spec`: 深度传感器规格
- `get_depth()`: 获取深度图方法（已实现）

### 2. 深度反投影

**SpatialSemanticFusionModule** 新增方法：

#### `_backproject_depth_to_world()`

将bbox从图像坐标反投影到世界坐标系：

1. **采样点提取**：
   - bbox中心点
   - 4个角点（左上、右上、左下、右下）

2. **深度值提取**：
   - 从深度图中提取5个采样点的深度值
   - 验证深度值有效性（0.1米到10米之间）
   - 使用中位数计算平均深度（更鲁棒）

3. **相机坐标系反投影**：
   ```
   Xc = (u - cx) * d / fx
   Yc = (v - cy) * d / fy
   Zc = d
   ```

4. **世界坐标系转换**：
   - 使用agent的旋转矩阵和位置
   - 将相机坐标系点转换到世界坐标系

5. **验证**：
   - 检查NaN/Inf值
   - 检查距离是否合理（不超过20米）

### 3. 观测类型标记

**add_observation()** 方法增强：

- 新增 `depth` 参数：可选的深度图
- 观测类型标记：
  - `"depth"`: 深度反投影成功
  - `"triangulation"`: 默认类型（需要三角化）
- 深度观测置信度提升：`confidence + 0.2`（最高1.0）
- 存储世界坐标：`world_position` 字段

### 4. 优先级策略

**estimate_object_position()** 方法更新：

**优先级顺序：**
1. **深度反投影**（如果有深度观测）
   - 单个深度观测：直接使用
   - 多个深度观测：按置信度加权平均
2. **三角化**（如果没有深度观测或深度观测不足）
   - 需要至少 `min_views` 个观测（默认3个）
   - 使用射线三角化方法

## 实现细节

### 深度值验证

```python
# 有效深度范围：0.1米到10米
if 0.1 <= d <= 10.0 and not np.isnan(d) and not np.isinf(d):
    depths.append(d)
```

### 采样点策略

- **中心点**：主要参考点
- **4个角点**：提供额外的深度信息，提高鲁棒性
- **中位数**：使用中位数而非平均值，对异常值更鲁棒

### 错误处理

所有深度反投影失败的情况都会记录日志：

1. **深度图尺寸不匹配**：警告但继续处理
2. **无效深度值**：记录具体位置和深度值
3. **有效深度值不足**：需要至少2个有效深度值
4. **反投影结果异常**：NaN/Inf或距离过远

## 使用示例

### 在环扫阶段

```python
# 获取深度图
depth = agent.get_depth()

# 检测到目标物体后
sweep_fusion_module.add_observation(
    position=pose["position"],
    rotation=pose["rotation"],
    bbox=bbox,
    confidence=conf,
    label=label,
    img_shape=(rgb.shape[0], rgb.shape[1]),
    depth=depth  # 传入深度图
)
```

### 在探索主循环

```python
# 获取深度图
depth = agent.get_depth()

# 检测到目标物体后
fusion_module.add_observation(
    position=pose["position"],
    rotation=pose["rotation"],
    bbox=bbox,
    confidence=conf,
    label=label,
    img_shape=(rgb.shape[0], rgb.shape[1]),
    depth=depth  # 传入深度图
)
```

## 日志输出

### 成功情况

```
空间融合模块：深度反投影成功，3D位置: [1.234, 2.345, 3.456], 置信度: 0.850
空间融合模块：添加观测 (类型: depth, 缓冲区大小: 1/10)
空间融合模块：使用深度反投影估计3D位置 (1.234, 2.345, 3.456), 平均置信度: 0.850 (深度观测数: 1)
```

### 失败情况

```
空间融合模块：无效深度值 d=0.050m 在 (320, 240)
空间融合模块：有效深度值不足 (1/5)，无法进行反投影
空间融合模块：深度反投影失败（无效深度值或超出范围）
空间融合模块：深度反投影距离过远: 25.67m
```

## 优势

1. **更直接**：不需要多个视角，单个深度观测即可定位
2. **更准确**：深度信息提供真实的距离测量
3. **更高置信度**：深度观测的置信度自动提升
4. **更快速**：不需要等待多个观测积累

## 限制

1. **深度图质量**：依赖深度传感器的精度和范围
2. **遮挡问题**：如果物体被遮挡，深度值可能不准确
3. **距离限制**：有效深度范围0.1-10米
4. **相机位姿**：假设相机与agent位置相同（简化处理）

## 未来改进

1. **相机相对位置**：从sensor_spec获取相机相对于agent的精确位置
2. **深度融合**：多个深度观测的融合策略优化
3. **深度-三角化融合**：结合深度和三角化的混合方法
4. **深度图预处理**：降噪、填充等预处理步骤

## 技术细节

### 坐标系转换

1. **像素坐标 → 相机坐标**：
   - 使用相机内参（fx, fy, cx, cy）
   - 深度值作为Z坐标

2. **相机坐标 → 世界坐标**：
   - 使用agent的旋转矩阵（四元数转换）
   - 加上agent的位置偏移

### 置信度计算

- **深度观测**：`min(1.0, visual_confidence + 0.2)`
- **三角化观测**：使用原始视觉置信度
- **多深度观测融合**：按置信度加权平均

